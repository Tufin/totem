version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.9
    working_directory: /go/src/github.com/Tufin/totem
    steps:
      - checkout
      - run: dep ensure
      - run: go test -v ./...